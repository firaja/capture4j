language: java

jdk:
- oraclejdk8

env:
  global:
  - JAVA_HOME=/usr/lib/jvm/java-8-oracle

install: true
sudo: false

script: "mvn clean install -P test-coverage"

after_success:
- bash <(curl -s https://codecov.io/bash)